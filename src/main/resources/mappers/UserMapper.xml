<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.item.vote.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.item.vote.bean.User">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="role" jdbcType="VARCHAR" property="role" />
    </resultMap>


    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
         *
        from USER
        where id = #{id,jdbcType=BIGINT}
    </select>
    <select id="selectUserList" resultMap="BaseResultMap">
         select *
           FROM
            USER
    </select>
    <select id="selectByUserName" parameterType="com.item.vote.bean.User" resultType="java.lang.Integer">
         select count(1)
           FROM
            USER
          where  name = #{name,jdbcType=VARCHAR}
    </select>
    <select id="selectRoleByUserName" parameterType="com.item.vote.bean.User" resultType="java.lang.String">
         select role
           FROM
            USER
          where  name = #{name,jdbcType=VARCHAR}
    </select>


    <select id="login" parameterType="com.item.vote.bean.User" resultType="java.lang.Integer">
         select count(1)
           FROM
            USER
            where
             name = #{name,jdbcType=VARCHAR}
             and password = #{password,jdbcType=VARCHAR}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
         delete from  USER
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="id" parameterType="com.item.vote.bean.User">
        insert into  USER
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">
                name,
            </if>
            <if test="password != null">
                password,
            </if>

        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                #{password,jdbcType=VARCHAR},
            </if>

        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.item.vote.bean.User">
        update USER
        <set>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                password = #{password,jdbcType=VARCHAR},
            </if>

        </set>
        where id = #{id,jdbcType=BIGINT}
        and  role != 'manager'
    </update>







</mapper>